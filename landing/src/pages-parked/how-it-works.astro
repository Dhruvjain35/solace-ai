---
/* The tour lives at /product now. This path was linked from the nav, the
   footer, the manifesto and the home page for months, so it redirects rather
   than 404s — a permanent redirect keeps whatever search equity the old URL
   accumulated and moves it to the new one. */
return Astro.redirect('/product', 301);
---
